# Image tag pointers — `make deploy` repoints these to a git sha; `make rollback`
# repoints them back. The local docker daemon's image store is the registry.
API_TAG=dev
WORKER_TAG=dev
DASHBOARD_TAG=dev
TRAFFIC_TAG=dev

# Host ports
PG_PORT=5432
GRAFANA_PORT=3000
API_PORT=8000
DASHBOARD_PORT=8080
