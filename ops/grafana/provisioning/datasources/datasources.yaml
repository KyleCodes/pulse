apiVersion: 1

datasources:
  - name: Postgres
    uid: postgres
    type: postgres
    access: proxy
    url: postgres:5432
    user: pulse
    isDefault: true
    editable: false
    jsonData:
      database: pulse
      sslmode: disable
    secureJsonData:
      password: dev

  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
