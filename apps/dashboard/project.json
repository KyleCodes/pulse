{
  "name": "dashboard",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t pulse-dashboard:${TAG:-dev} apps/dashboard",
        "cwd": "{workspaceRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./run-dev.sh",
        "cwd": "{projectRoot}"
      }
    }
  }
}
