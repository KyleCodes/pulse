server {
  listen 80;

  # Same-origin proxy to the api service (compose DNS). No CORS anywhere.
  location /api/ {
    proxy_pass http://api:8000/;
  }

  location / {
    root /usr/share/nginx/html;
    index index.html;
  }
}
