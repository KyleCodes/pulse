{
  "name": "traffic",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t pulse-traffic:${TAG:-dev} apps/traffic",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
