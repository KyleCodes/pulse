{
  "name": "worker",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t pulse-worker:${TAG:-dev} apps/worker",
        "cwd": "{workspaceRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./run-dev.sh",
        "cwd": "{projectRoot}"
      }
    }
  }
}
